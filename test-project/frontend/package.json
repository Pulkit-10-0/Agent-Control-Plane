{
  "name": "test-frontend",
  "version": "1.0.0",
  "description": "Test frontend for ACP tracing demo",
  "scripts": {
    "start": "npx serve public -l 3000",
    "dev": "npx serve public -l 3000"
  },
  "devDependencies": {
    "serve": "^14.2.1"
  }
}